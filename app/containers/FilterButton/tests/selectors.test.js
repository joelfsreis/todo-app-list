// import { fromJS } from 'immutable';
// import { makeSelectFilterButtonDomain } from '../selectors';

// const selector = makeSelectFilterButtonDomain();

// TODO: write some tests...
describe('makeSelectFilterButtonDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
