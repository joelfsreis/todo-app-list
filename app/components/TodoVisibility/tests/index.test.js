// import React from 'react';
// import { shallow } from 'enzyme';

// import TodoVisibility from '../index';

// TODO: write some tests...
describe('<TodoVisibility />', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
